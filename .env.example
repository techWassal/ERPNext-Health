# =============================================================================
# ERPNext + Healthcare + HRMS + Helpdesk + ZATCA Environment Configuration
# =============================================================================
# IMPORTANT: Copy this file to .env and modify values before deployment
# NEVER commit .env file to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Site Configuration
# -----------------------------------------------------------------------------
# Your site name (domain or subdomain)
SITE_NAME=erp.example.com

# Admin password for the ERPNext Administrator account
# CHANGE THIS IMMEDIATELY - use a strong password (16+ characters)
ADMIN_PASSWORD=ChangeMeToAStrongPassword123!

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MariaDB root password - used for database administration
# CHANGE THIS - use a strong password (16+ characters)
DB_ROOT_PASSWORD=ChangeThisToSecureRootPassword456!

# MariaDB host (internal Docker network name)
DB_HOST=db

# MariaDB port (internal)
DB_PORT=3306

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis hosts (internal Docker network names)
REDIS_CACHE=redis-cache:6379
REDIS_QUEUE=redis-queue:6379

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Frappe/ERPNext version tag for the custom image
ERPNEXT_VERSION=v15-healthcare

# Number of Gunicorn workers (recommended: 2 * CPU cores + 1)
# Small (1-50 users): 2-4 workers
# Medium (50-500 users): 4-8 workers
# Large (500+ users): 8-16 workers
WORKERS=4

# Background worker configuration
WORKER_COUNT_DEFAULT=1
WORKER_COUNT_SHORT=1
WORKER_COUNT_LONG=1

# -----------------------------------------------------------------------------
# Installed Apps (in order of installation)
# -----------------------------------------------------------------------------
# 1. ERPNext          - Core ERP
# 2. Frappe Payments  - Payment Gateway Integration
# 3. Frappe HRMS      - Human Resource Management
# 4. Healthcare       - Frappe Healthcare (Marley)
# 5. Telephony        - Call Management
# 6. Helpdesk         - Support Ticket System
# 7. ZATCA            - Saudi Arabia e-invoicing

# -----------------------------------------------------------------------------
# Security Settings
# -----------------------------------------------------------------------------
# Encryption key for passwords (auto-generated during site creation)
# Leave empty for auto-generation, or set for consistency across restores
ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# Email Configuration (Optional - for notifications)
# -----------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_LOGIN=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_USE_TLS=1

# -----------------------------------------------------------------------------
# Backup Configuration
# -----------------------------------------------------------------------------
# Backup retention (days)
BACKUP_RETENTION_DAILY=7
BACKUP_RETENTION_WEEKLY=4
BACKUP_RETENTION_MONTHLY=3

# Off-site backup (S3-compatible storage)
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_BUCKET=your-backup-bucket
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_REGION=us-east-1

# -----------------------------------------------------------------------------
# Resource Limits (adjust based on server specs)
# -----------------------------------------------------------------------------
# Memory limits (format: 512m, 1g, 2g, etc.)
BACKEND_MEMORY_LIMIT=2g
FRONTEND_MEMORY_LIMIT=512m
DB_MEMORY_LIMIT=2g
REDIS_MEMORY_LIMIT=256m

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log directory (inside container)
LOG_DIR=/home/frappe/frappe-bench/logs
